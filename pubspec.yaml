name: my_library_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  # База
  sqflite: ^2.3.3
  path: ^1.9.0
  path_provider: ^2.1.4

  # Импорт файлов
  file_picker: ^8.1.2

  # Настройки
  shared_preferences: ^2.2.3

  # PDF
  flutter_pdfview: ^1.3.2

  # DOCX парсинг своими силами
  archive: ^3.6.1
  xml: ^6.5.0

flutter:
  uses-material-design: true
